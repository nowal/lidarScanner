# Live end-to-end runs, 2026-09-10

Every run below drove a real server process (uvicorn, the startup-owned
email worker, `LIDARAI_STRICT_CONFIG` off, every mail transport blanked so
the lead email is captured in `{storage}/ops_outbox/`), the live model
(Anthropic claude-sonnet-5, effort low), and the development Supabase
project (flow state, journal, quote requests, provider table, home indexes
and room models). Homeowner data is synthetic (555 numbers, example.com
addresses, "118 Maple Street"); the whole-home scan is a real export whose
owner consented to its use in testing. Test provider rows written to the
development table were deleted after each run.

Drivers: `scripts/e2e/` (see its README). Each prints a PASS/FAIL checklist.

## Operations email, restart mid-flow (`e2e_ops_email.py`)

| Phase | Result |
|---|---|
| 1 — first lead: six live turns → 201 → ops view without thread/homeowner/job ids, address withheld → lead email composed by the worker (ranked section, entry link, no address) → entry page opens → ops upload of two quotes + one illustrative estimate → provider table holds both as quoted ×1, the estimate not promoted → agent presents both quotes, request `presented` | 16/16 |
| 2 — server stopped, storage wiped, restarted: startup log `Provider table at startup: rehydrated` → second lead's email marks both providers `[PAST QUOTER x1]`, no sample seed rows, HTML badge present | 14/14 |

## Scope intent, three scopes × two flag sources (`e2e_scope.py`)

| Run | Signal | Result |
|---|---|---|
| `single_room` | `processor_job` | scope captured; gate closed while processing with no scan suggestion delivered; once open, mode `generic_once`; package `one room (living room)` | 18/18 (one enforcement regeneration, delivered reply clean) |
| `selected_rooms` (this room + kitchen) | `processor_job` | mode `named_rooms`; reply stayed within the two rooms; subject `Painting — this room + kitchen in 37203` | 19/19 |
| `whole_home` | `processor_job` | mode `open`; the agent invited the next space with the continuity guidance; email says `whole home` | 18/18 |
| `selected_rooms` | `device_bake` | gate held while `localModelReady: false` despite `processingState: complete`, opened on `true`; journal `scanSignal: device_bake` | 20/20 |

## Whole-home scan, real export `scan-5B579D84` (`e2e_home.py`)

Eight areas, one storey, 1,879 sq ft. `bundle_check.py`: PASS (three areas
needed the homeowner's name; one room bake at the 50 MB cap; whole-home
bake 233 MB, not stored). Ingested with seven room models in Supabase.

| Check | Result |
|---|---|
| ops listing shows the new home and the two earlier ones | pass |
| opening at whole-home level names real rooms, asks for a name, no fallback | pass |
| "let's start with the kitchen" → kitchen is the active room | pass |
| "what about the garage" → echoed as unresolved, never described as seen; no scan suggestion while processing | pass |
| "that space you called unnamed area 1 is the mudroom" → renamed in the index | pass (after fix) |
| scope `selected_rooms` with the real rooms; a re-statement replaces the list | pass (after fix) |
| hard rule under an explicit "walk the rest of the house now?" push | pass |
| flag set → mode `named_rooms`, no rest-of-home invitation | pass |
| steps 7–9 → 201; package has kitchen-level measurements (paintable wall area), scope, a signed URL to the stored kitchen bake, no ids, address withheld | pass |
| lead email: scope line, model link, subject `Painting — kitchen + bathroom in 37203`, no address | pass |
| switch to `quintin-house` mid-thread → fresh subject, the agent acknowledges the switch and lists that home's rooms | pass (after fix) |
| ops upload → agent presents the quote | pass |

Final: 22/22, repeated after the branch review fixes: 22/22.

## Sample scans on hand

The SOW names five sample scans as the acceptance substrate. Available
today: the app's `captured-room.json` single-room fixture, `noah-house`
(19 rooms, two storeys), `quintin-house` (6 rooms), and `5B579D84`
(8 rooms). One further bundle received earlier (`C79DE904`) carried frames
only and is not ingestable. **Two more usable whole-home exports are needed
from TakeShape (§8) to run the acceptance criterion as written.**
